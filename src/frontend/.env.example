# Copy to .env.local (gitignored) or set in your deploy environment.
# These values are baked into the bundle at build time — they are NOT secrets
# in the security sense, they're public configuration. The X-Api-Key header
# value is visible to anyone who opens DevTools.
VITE_API_BASE=
VITE_API_KEY=
